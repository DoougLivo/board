server:
  port: 8090

spring:
  thymeleaf:
  prefix: classpath:/templates/
  suffix: .html

  h2:
    console:
      enabled: true

  datasource:
    hikari:
      maximum-pool-size=4:
    url: jdbc:postgresql://127.0.0.1:5432/cyd_database
    driver-class-name: org.postgresql.Driver
    username: cyd
    password: 1234

  # ???? spring.io?? doc?? ??? ??????.
  # jpa??
  jpa:
    hibernate: # hibernate ?? ??
      # ?????? ?? ??? ???? ? ???, ?? ?? entity ??? ?? ?? ??? ?? ??
      # if exists drop table ??? ?? ?????? ?? ??.
      ddl-auto: update
    properties: # property ?? ??
      hibernate: # hibernate property ??
        format_sql: true

# ?? ?? ??
logging:
  level:
    # hibernate ? ??? ?? ??? debug??? ??
    # jpa hibernate? ???? sql? ??? ??? ???? ?? ??
    org.hibernate.SQL: debug
    org.hibernate.type: trace # ?? ???? ????? ???? ??